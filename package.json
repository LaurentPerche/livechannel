{
  "name": "driftyt",
  "version": "0.1.0",
  "private": true,
  "description": "DriftYT turns YouTube subscriptions into a continuous personal video channel on Cloudflare Workers.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --test-scheduled",
    "deploy": "wrangler deploy",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260430.0",
    "typescript": "^5.8.3",
    "wrangler": "^4.14.1"
  }
}
